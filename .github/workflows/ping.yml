name: Keep HF Space Alive

on:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Space
        run: curl -s https://huggingface.co/spaces/sachuretnasm/article-research-tool
